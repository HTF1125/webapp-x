



export const API_URL = process.env.API_URL || ""; // Ensure this is correctly set in your environment
export const NEXT_PUBLIC_API_URL = process.env.NEXT_PUBLIC_API_URL || ""; // Ensure this is correctly set in your environment
